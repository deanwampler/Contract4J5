ant directory README

The XML files here are used by the build process.

The "ajc.args.lst" file contains command line arguments for AspectJ's "ajc" for which there
are no properties yet defined for the corresponding "<iajc>" ant task. This file is read
by ajc.

